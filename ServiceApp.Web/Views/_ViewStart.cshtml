@* =================================================================
   _ViewStart.cshtml
   
   Runs before every view. Sets the default layout.
   Auth views (Login, Register) override this by setting:
       Layout = "~/Views/Shared/_AuthLayout.cshtml";
   ================================================================= *@

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}